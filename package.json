{
  "name": "pong-svg",
  "version": "0.1.0",
  "description": "Your regular SVG Pong game.",
  "license": "ISC",
  "author": "Adham <1302313>",
  "main": "src/index.js",
  "scripts": {
    "start": "parcel index.html -p 3000",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /YOUR-REPO-NAME",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.3"
  }
}
